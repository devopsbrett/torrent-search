<%= form_tag(search_path, method: "get") do %>

  <div class="field">
    <%= label_tag(:q, "Search for:") %>
    <%= text_field_tag(:q, @torrent ? @torrent.name : "") %>
  </div>
  <div class="actions">
    <%= submit_tag("Search") %>
  </div>
<% end %>
